import { ObjectPageConfig } from '../../../../v2';
export declare function createObjectPageConfig(manifest: object, flex: string[], jsonSchema: object, entitySet: string, entityAnnotations?: object): ObjectPageConfig;
